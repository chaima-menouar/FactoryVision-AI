# Hosted training dependencies (Kaggle/Colab only)
# PyTorch is installed separately because Kaggle GPU compatibility depends on the CUDA wheel.
# For Tesla P100 (Pascal, sm_60), use the CUDA 12.6 PyTorch wheel.
anomalib>=2.2,<3
lightning>=2.5,<3
timm>=1.0
scikit-learn>=1.6
pandas>=2.2
matplotlib>=3.9
pyyaml>=6.0
