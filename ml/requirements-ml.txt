# Hosted training dependencies (Kaggle/Colab only)
# PyTorch is supplied by the hosted notebook environment.
# Prefer a Tesla T4-class accelerator on Kaggle when the preinstalled PyTorch build does not support P100 (sm_60).
anomalib>=2.2,<3
lightning>=2.5,<3
timm>=1.0
scikit-learn>=1.6
pandas>=2.2
matplotlib>=3.9
pyyaml>=6.0
